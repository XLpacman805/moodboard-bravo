extends layout

block content
    h1= 'Moodboard Bravo'
    h2= test
    p Welcome to #{title}

    div(id="drop_zone" ondrop="dropHandler(event);" ondragover="dragOverHandler(event);")
        p drop one or more files to this drop zone.