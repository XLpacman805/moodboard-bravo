extends layout

block content
    h1= 'Moodboard Bravo'
    h2= test
    p Welcome to #{title}
    form(action='/moodboard')
        p
            | ENTER IMGUR ALBUM INK:
            input(type='text', name='albumlink', value='' id='albumlink')
            button(id='submit-button') Submit

    script(src='/javascripts/index.js')